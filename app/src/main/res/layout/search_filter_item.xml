<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:paddingStart="@dimen/_5sdp"
    android:paddingEnd="@dimen/_5sdp"
    android:background="@color/White">
    <TextView
        android:id="@+id/search_filter_title_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:text="@string/dash"
        />

    <Spinner
        android:id="@+id/search_filter_spinner"
        style="@style/SearchSpinner"
        app:layout_constraintStart_toStartOf="@id/search_filter_title_tv"
        android:layout_marginTop="@dimen/_5sdp"
        android:background="@drawable/gradient_spinner"
        app:layout_constraintTop_toBottomOf="@id/search_filter_title_tv"
        android:spinnerMode="dropdown"
        />
</androidx.constraintlayout.widget.ConstraintLayout>